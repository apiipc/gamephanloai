# Railway: Root Directory = apps/web (bắt buộc nếu deploy từ GitHub repo)
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.web"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
