# Railway (legacy): deploy **chỉ** frontend tĩnh — dùng khi tách service `web`.
# Khuyến nghị: một service với Dockerfile ở gốc repo (không cần file này).

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.web"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
