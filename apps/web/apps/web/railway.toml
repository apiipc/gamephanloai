# Bản sao cho deploy CLI `--path-as-root`: Railway vẫn tìm `apps/web/railway.toml`.
# File chính: ../../railway.toml (cùng nội dung).
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
