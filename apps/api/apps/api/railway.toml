# Bản sao cho deploy CLI `--path-as-root`: Railway vẫn tìm đường dẫn giống monorepo `apps/api/railway.toml`.
# File chính: ../../railway.toml (cùng nội dung).
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
